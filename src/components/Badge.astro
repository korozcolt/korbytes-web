---
type BadgeKind = "production" | "development" | "concept" | "success";

interface Props {
  label: string;
  kind?: BadgeKind;
}

const { label, kind = "production" } = Astro.props;
---

<span class={`kb-badge badge-${kind}`}>{label}</span>
